import 'package:flutter/material.dart';
// page dss kit de parametre vitaux

class Kit extends StatelessWidget {
  const Kit({super.key});

   Widget build(BuildContext context) {
    return Scaffold(
      appBar: 
      AppBar(
        title: Text('kit de suivie'),
      ),
     body: Center(child: Text('Nos service de kit de suivie  ')),
    );
    
  }
}
